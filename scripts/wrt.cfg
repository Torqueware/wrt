/******************************************************************************
 * wrt.cfg                                                                    *
 *                                                                            *
 * Copyright 2013 William Patrick Millard <wmillard1@gmail.com>               *
 *                                                                            *
 * WRT configuration file version 0.1                                         * 
 *                                                                            *
 * This file is used to store data on managed APs.                            *
 * This file also holds basic local config settings ('local flavour')         *
 *                                                                            *
 * Note: libconfig strips comments when it writes to a configuration          *
 *       file. IE. By virtue of this comment, you can be assured that         *
 *       this example configuration was written by a human.                   *
 *                                                                            *
 ******************************************************************************/

/**
 * Directory WRT searches for certificates, and the configuration file
 * (Default is /etc/wrt/)
 *
 * @type {String}
 */
Config_Dir   = "/etc/wrt/"

/**
 * Directory WRT saves it's log to - naming convention used: 'wrt.log'
 *
 * @type {String}
 */
Log_Dir      = "/var/log/"

/**
 * EUID to run WRT as on the local machine (EXPERIMENTAL)
 *
 * @type {String}
 */
local_user  = "wrt"

/**
 * User to authenticate as on the remote machine.
 *
 * @type {String}
 */
remote_user = "root"

/** 
 */
/**
 * Log level - controls the verbosity of output DISPLAYED on standard out
 * Log Levels:
 * <1 - Brief
 *  1 - Default
 *  2 - Verbose
 * >2 - Very Verbose
 *
 * @type {Number}
 */
Log_Level   = 1;

/**
 * Whether or not to create a PID file in the configuration directory
 * @type {Boolean}
 */
PID_file    = false;



/**
 * SSID - the 802.11 service group name used by conforming nodes
 * @type {String}
 */
SSID        = "test_mesh"

/**
 * Encryption used (Default: WPA / WPA2)
 * @type {String}
 */
Encryption  = "WPA"

/**
 * Password used to recieve service from compliant nodes
 * @type {String}
 */
Passwprd    = "knockknock"

/**
 * Access_Points holds a list of WRT compliant nodes
 * Each node has five fields: Name, Type, MAC, IPv4, and IPv6
 * @type {[type]}
 */
Access_Points =
(
/*  Example listing for a list of length 2 containing
    APs example1 and example2.
    { Name = "example1";
      Type = "none"
      MAC  = "1A:2b:3C:4d:5E:6f";
      IPv4 = "192.168.1.123";
      IPv6 = "2001::dead:beef"; }, <--- note the ','

    { Name = "example2";
      MAC  = "1A:2b:3C:4d:5E:6f";
      IPv4 = "192.168.1.124";
      IPv6 = "2001::dead:beff"; }  <--- note the lack of ','
*/

    { Name = "example";
      MAC  = "1A:2b:3C:4d:5E:6f";
      IPv4 = "192.168.1.123";
      IPv6 = "2001::dead:beef"; },

    { Name = "micro1";
      MAC  = "14:CF:92:A1:0A:74";
      IPv4 = "TODO";
      IPv6 = "TODO"; },

    { Name = "wrt1";
      MAC  = "2A:2b:3C:4d:5E:6f";
      IPv4 = "TODO";
      IPv6 = "TODO"; }
      
);

